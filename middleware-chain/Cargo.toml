[package]
name = "middleware-chain"
version = "0.1.0"
edition = "2021"

[dependencies]
rustapi-rs = { version = "0.1.233", features = ["jwt", "cors"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
utoipa = "4.2"
http = "1"
