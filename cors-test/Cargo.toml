[package]
name = "cors-test"
version = "0.1.0"
edition = "2021"

[dependencies]
rustapi-rs = { version = "0.1.236", features = ["cors", "rate-limit"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
